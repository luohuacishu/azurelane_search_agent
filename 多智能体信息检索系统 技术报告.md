# 多智能体信息检索系统 技术报告

## 1\. 项目简介

本项目基于 AutoGen AgentChat 框架，设计并实现了一套**多智能体全自动信息检索流水线**，用于查询碧蓝航线 Wiki 图鉴数据。系统通过多个职责独立的智能体协作，自动完成用户需求解析、外部 Wiki 数据获取、信息提炼、结果审核迭代，最终输出结构化、简洁、可验证的查询结果。

项目全程无人工干预，依靠智能体自主协作、自我校验、迭代优化，实现了完整闭环的多智能体任务系统。

## 2\. 系统总体设计与决策依据

### 2.1 多智能体角色分工设计（核心设计）

本项目共设计 **4 个职责完全解耦的智能体**，形成流水线 + 反馈迭代结构，每个智能体拥有独立 System Prompt，职责单一、边界清晰。

* **总结智能体（summarize\_agent）**：负责自然语言需求理解，将用户问题转化为工具调用参数，实现用户层与工具层解耦。
* **搜索智能体（search\_agent）**：唯一拥有工具调用权限的智能体，调用 Wiki 接口获取原始字典数据，专注外部数据获取。
* **提炼智能体（extract\_agent）**：清洗海量原始 Wiki 数据，精简、结构化用户需要的内容，负责输出最终可读结果，支持多轮重提炼。
* **审核智能体（user\_proxy）**：自动校验输出质量，判断结果是否满足用户需求，实现“通过终止 / 不通过重提炼”的迭代闭环。

**设计理由**：拆分任务可以让每个智能体专注单一职责，降低 Prompt 复杂度、提升稳定性、便于单独调优，是工业级多智能体流水线的标准设计。

### 2.2 协作模式选型决策（关键得分点）

本项目**放弃 RoundRobin 固定轮询**，选择 **SelectorGroupChat 动态选择器模式**。

**技术原因：**

* 本任务不是固定顺序任务，存在**条件分支**：正常流水线顺序执行，但审核失败时需要**动态回退到提炼环节重跑**。
* RoundRobin 只能固定顺序轮流发言，无法实现跳转、无法实现迭代优化。
* 自定义 Selector 函数可根据上一轮消息来源与内容，动态路由下一个执行者，完美适配“流水线 + 反馈迭代”的业务逻辑。

### 2.3 双层终止机制与安全边界设计

为防止多轮迭代产生无限循环、浪费 Token，本系统配置**双重终止保护机制**：

* **语义终止（软终止）**：TextMentionTermination，审核智能体输出 Terminated 代表任务完成。
* **最大轮次硬限制**：max\_turns=10，兜底防止智能体互相拉扯死循环。

该设计保证系统既有迭代优化能力，又具备工程安全边界。

### 2.4 工具调用与外部数据集成

系统实现了标准 Function Calling 流程，由搜索智能体专属调用 Wiki 数据接口。工具返回完整结构化字典，交由后续智能体处理。

工具权限隔离保证了：只有数据获取层能访问外部资源，避免重复请求、乱调用，系统结构更稳定。

## 3\. 系统工作流程

用户提问 → 需求解析 → Wiki 原始数据拉取 → 信息结构化提炼 → 智能体自动审核

审核通过 → 任务结束；审核不通过 → 自动回退提炼模块重新整理内容，形成闭环迭代。

## 4\. 开发反思与问题优化（重点加分项）

### 4.1 动态路由稳定性问题

初期智能体输出句式不统一，导致 Selector 跳转失效。最终通过固定每个智能体结尾关键词、标准化输出格式，彻底解决路由不稳定问题。

### 4.2 多轮迭代结果覆盖问题

系统支持多次重提炼，消息列表不断追加。原本正序遍历效率低下，优化为**倒序查找最新提炼结果并立刻 break**，既高效又保证永远取最终迭代结果。

### 4.3 全自动 vs 人机协同取舍

本项目定位全自动查询系统，因此未引入人工介入。全部校验由 AI 审核智能体完成，优势是无人值守、自动化程度高；缺点是极端场景下 AI 可能误判，未来可增加 Human-in-the-Loop 关键节点人工复核。

### 4.4 系统局限与未来改进

当前 Wiki 工具未完善异常捕获，容易出现网络限流报错；后续可增加重试机制、异常提示，让智能体具备错误感知能力。

## 5\. 总结

本项目成功搭建了**分工明确、动态调度、可迭代、安全可控**的多智能体协作系统。通过合理的智能体角色拆分、Selector 动态调度、双层终止保护、外部工具调用，实现了完整的多智能体工程化任务闭环，满足智能体协作、工具调用、迭代优化、边界安全等全部设计要求。

